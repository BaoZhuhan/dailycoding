#include <bits/stdc++.h>
using namespace std ;

int main(){
    ios::sync_with_stdio(0),cin.tie(0) ;

    int n ; cin >> n;

    set<int> q ; for(int i = 0 ; i <)
    vector<int> a(n) ;

    for(int i = 0 ; i < n ; i++) {
        cin >> a[i];

        if(i != 0 ) {

        } 
    }


    return 0 ;
}