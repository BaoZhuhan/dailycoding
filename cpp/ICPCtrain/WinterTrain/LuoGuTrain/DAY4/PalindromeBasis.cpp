#include <bits/stdc++.h>
using namespace std;

bool check(int x){
    vector<int> c;
    while(x) {
        c.push_back(x%10);
        x /= 10;
    }
    for(int i = 0 , j = c.size() - 1 ;i < j ;i++ , j--){
        if(c[i] )
    }
}

void pre(){
    for(int i = 1 ; i < 40000 ; i++){
        if(check(i)) a[++t] = i;
    }
    for(int i = 1 ; i <= 40000 ; i++){
        for(int j = 1; j<= t ; j++) {
            if()
        }
    }
}


int main(){
    ios::sync_with_stdio(0)
}