#include <bits/stdc++.h>

using namespace std ;


void run(){
    int n ; 
    cin >> n;

    vector<int> a(n);
    for(int i = 0 , i < n ; i ++) {
        cin >> 
    }

}
int main(){
    ios::sync_with_stdio(0),cin.tie(0);
    run();
    return 0;
}