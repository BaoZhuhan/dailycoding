#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    void dfs(vector<vector<int> >& isConnected , vector<int>& visited , int city , int i){
        for(int j = 0 ; j < city ;j++) {
            if(isConnected[i][j] == 1 and )
        }
    }
    int findCircleNum(vector<vector<int>>& isConnected) {
        
    }
};
int main(){
    Solution s;
    return 0;
}